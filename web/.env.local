# Local environment (APP_ENV=local, the default). Committed as the dev default.
DB_PATH=data/local/db.sqlite3
JWT_SECRET=7598295b798044189fa1df601e0d2c37a379ec30fbf74b1189431663a2be4064

# DEV license private key — the pair of the committed scripts/license-pubkey.bin,
# so the whole buy→sign→verify flow runs locally out of the box. This is NOT a
# secret (it's committed); NEVER reuse it in prod. Generate a fresh, secret key
# for production with `bun run scripts/gen-license-keypair.ts`.
LICENSE_PRIVKEY_B64=MC4CAQAwBQYDK2VwBCIEIB+rwirWg2ELEpf3ukxXoj6g5PNUVlGNJ8N2In1JLnOE

# Stripe test-mode placeholders — drop your own sk_test_/price_/whsec_ values in.
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PRICE_ID=price_...
STRIPE_WEBHOOK_SECRET=whsec_...
PUBLIC_BASE_URL=http://localhost:3001
