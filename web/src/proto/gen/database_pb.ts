// The database schema — the storage layout for the whole app, as one `Database`
// message where every field is `repeated <Table>` (a table is a set of rows;
// see protodatabase/README.md). Each table is a model in models/; this file
// only assembles them into the schema registry that protodatabase reads.
//
// Owner-scoped: every domain row carries an `owner` (a User id), so the DB is a
// set of per-user rows. `users` is the one exception (it IS the accounts).
//
// Physical tables are keyed by field NUMBER, so never change a field number or
// name here without a migration. Retired tables leave a gap rather than
// renumber a surviving table onto an occupied physical table.
//
// Generate:  bun run gen:db  (see package.json)

// @generated by protoc-gen-es v2.12.1 with parameter "target=ts"
// @generated from file database.proto (package app.db.v1, syntax proto3)
/* eslint-disable */

import type { GenFile, GenMessage } from "@bufbuild/protobuf/codegenv2";
import { fileDesc, messageDesc } from "@bufbuild/protobuf/codegenv2";
import type { User } from "./models/user_pb";
import { file_models_user } from "./models/user_pb";
import type { Item } from "./models/item_pb";
import { file_models_item } from "./models/item_pb";
import type { Message } from "@bufbuild/protobuf";

/**
 * Describes the file database.proto.
 */
export const file_database: GenFile = /*@__PURE__*/
  fileDesc("Cg5kYXRhYmFzZS5wcm90bxIJYXBwLmRiLnYxIkQKCERhdGFiYXNlEhsKBXVzZXJzGAEgAygLMgwuYXBwLnYxLlVzZXISGwoFaXRlbXMYAiADKAsyDC5hcHAudjEuSXRlbWIGcHJvdG8z", [file_models_user, file_models_item]);

/**
 * @generated from message app.db.v1.Database
 */
export type Database = Message<"app.db.v1.Database"> & {
  /**
   * accounts
   *
   * @generated from field: repeated app.v1.User users = 1;
   */
  users: User[];

  /**
   * PK = id, owner-scoped example table
   *
   * @generated from field: repeated app.v1.Item items = 2;
   */
  items: Item[];
};

/**
 * Describes the message app.db.v1.Database.
 * Use `create(DatabaseSchema)` to create a new message.
 */
export const DatabaseSchema: GenMessage<Database> = /*@__PURE__*/
  messageDesc(file_database, 0);

